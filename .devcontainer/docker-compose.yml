version: "3.9"
services:
  frontend:
    build:
      context: ../
      dockerfile: Dockerfile

    volumes:
      - ../:/workspace
      - /workspace/node_modules
      - /workspace/.next
